# K3s - CVE scans portal

# Severities

Only critical and high severity CVEs are displayed. CVEs that had their CVSS severity rating changed, either decreased
or increased, will have the distinctive tag `*` close to its severity.

# VEX and false-positives

Known false-positives CVEs are displayed in the portal with its severity as
`none` and the status as `not affected`.

# Attribution

VEX data is provided by SUSE under the CC BY 4.0 license.